info:
  name: Retrieve Workflow Instances
  type: http
  seq: 3

http:
  method: GET
  url: "{{eventbus_prime_url}}/api/work/instances"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
