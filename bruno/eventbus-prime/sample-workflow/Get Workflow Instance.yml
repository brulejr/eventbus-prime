info:
  name: Get Workflow Instance
  type: http
  seq: 3

http:
  method: GET
  url: "{{eventbus_prime_url}}/api/work/instances/:instanceId"
  params:
    - name: instanceId
      value: d81d37be-049c-498a-9b3f-912f7d172ceb
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
